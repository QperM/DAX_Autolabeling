apiVersion: apps/v1
kind: Deployment
metadata:
  name: dax-pose
  namespace: dax-autolabeling
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dax-pose
  template:
    metadata:
      labels:
        app: dax-pose
    spec:
      containers:
        - name: pose
          # TODO: replace with your pushed image
          image: registry.daxrobotics.cn/auto-labeling-tool/dax-pose:<tag>
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 7900
          env:
            - name: DATA_ROOT
              value: "/app"
            - name: POSE_SERVICE_PORT
              value: "7900"
            - name: CUDA_DEVICE_ORDER
              value: PCI_BUS_ID
          readinessProbe:
            httpGet:
              path: /health
              port: 7900
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 7900
            initialDelaySeconds: 30
            periodSeconds: 20
          volumeMounts:
            - name: uploads
              mountPath: /app/uploads
          resources:
            requests:
              cpu: "1"
              memory: 4Gi
            limits:
              cpu: "4"
              memory: 16Gi
              nvidia.com/gpu: "1"
      volumes:
        - name: uploads
          persistentVolumeClaim:
            claimName: dax-uploads-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: dax-pose
  namespace: dax-autolabeling
spec:
  selector:
    app: dax-pose
  ports:
    - name: http
      port: 7900
      targetPort: 7900

